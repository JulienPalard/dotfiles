order += "apt_updates"
order += "loadavg"
order += "networkmanager"
order += "battery_level"
order += "diskdata"
order += "volume_status"
order += "xrandr"
order += "tztime local"

py3status {
    separator = True
    separator_block_width = 21
}

tztime local {
    format = "%Y-%m-%d %H:%M"
}
