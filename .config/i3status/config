order += "apt_updates"
order += "loadavg"
order += "networkmanager"
order += "battery_level"
order += "diskdata"
order += "volume_status"
order += "xrandr"
order += "tztime local"

tztime local {
    format = "%Y-%m-%d %H:%M"
}
